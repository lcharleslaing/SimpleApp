// preload.js
window.addEventListener('DOMContentLoaded', () => {
  // This is where you can expose specific APIs from Node.js to the renderer process
  // For now, we're just setting up the basic security model
  console.log('Application loaded successfully')
})